//дёрнул готовый хелуворлд из интернетов
//Я не проггер но тут всё очевидно, подключили заголовок, в теле  последовательность 
//действий. puts (видимо put string) печатает строку на стандартный вывод а retur возвращает 0
//что значит "всё ок".
#include <stdio.h>

int main (void)
{
	puts ("Hello, World!");
	return 0;
}
